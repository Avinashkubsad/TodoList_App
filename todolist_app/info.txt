1. Create a new React app with below command
 npm create vite@latest todolist_app

2. install  Dependencies using 
ES7+ React/Redux/React-Native snippets   ---> for rafc to create a component

3. App.js i smain ntry point for reactjs
in if we use <> </> then it is called fragments so that we can write in that what we have to display in the browser


4. ClassName is very important in this 

5. vite.config.js is the config u can make changes so that it will effect whole project 
   ex :     export default defineConfig({
            plugins: [react()],
            server: {
            port: 3000,   // 👈 change this to whatever you want
            open: true    // (optional) auto-opens browser
           }
        })


   //TodoForm
6. useState -> To keep track whatever the user is Typing 

7. when you type  something and submit then the page reloads automatically
   so to prevent that we should use event   e.preventDefault();

 --> after that i have to pass the value to todowrapper parent component
 i.e use/import useState in parent component

 8 **Important**
   --> if you want to pass the values then use props 

   | Direction      | Method                | Example                  |
| -------------- | --------------------- | ------------------------ |
| Parent → Child | Props                 | `<Child data={value} />` |
| Child → Parent | Callback              | `onChange(value)`        |
| Both ways      | Controlled components | Input example above      |


// Parent
<Child value={text} onChange={setText} />

// Child
<input value={value} onChange={(e) => onChange(e.target.value)} />








        